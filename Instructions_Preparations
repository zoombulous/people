# Install Vue CLI
      npm install -g @vue/cli

# Create New Vue project
      vue create PEOPLELIST
      select default configuration
  
# Change Directory
      cd PEOPLELIST
      
# Inside of PROJECTNAME, paste a JSON file (people.json) or create one and input data

# Run Vue CLI
      npm run serve
  
# Install JSON-Server (may do locally but theese instructions are for global install)
      npm i json-server -g
  
# Run application
      cd PROJECTNAME
      json-server db.json
      If this works, you should get a message that reads \{^_^}/ hi!
      
# Install Axios (this also can be installed locally, but we are going to install it globally)
      npm i axios
